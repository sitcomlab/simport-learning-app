import { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
 appId: 'de.ifgi.simport.learning',
  appName: 'simport-learning',
  bundledWebRuntime: false,
  webDir: 'www',
}

export default config
